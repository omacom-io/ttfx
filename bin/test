#!/usr/bin/env sh
set -eu

ROOT="$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)"

if ! command -v cargo >/dev/null 2>&1; then
  echo "cargo is required to run the ttfx tests." >&2
  exit 1
fi

cd "$ROOT"
cargo test "$@"
